# Docker Compose Configuration for Sellia POS
# Add your services here (e.g., PostgreSQL, Redis, etc.)

services:
  # Add services as needed
  # Example:
  # postgres:
  #   image: postgres:15
  #   container_name: sellia-postgres
  #   environment:
  #     POSTGRES_DB: sellia_db
  #     POSTGRES_USER: sellia_user
  #     POSTGRES_PASSWORD: your_password
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - sellia-network

# volumes:
  # postgres_data:
  #   driver: local

networks:
  sellia-network:
    driver: bridge
